import {
  constantRoutes
} from '@/router'
const state = {
  routes: constantRoutes // 静态lul
}
const mutations = {}
const actions = {}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
